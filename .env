# Change to point to the correct directories for your system

# (inventory-api repository)/data/schema/resources
JSONSCHEMA_OUTPUT_DIR=output/jsonschema
# (rbac-config repository)/configs/(stage|prod)/schemas/src
KSL_OUTPUT_DIR=output/ksl