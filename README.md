# Starlark Unified Schema

This is a prototype unified schema model (including tooling) that produces build artifacts that become inputs to Kessel. This is a transitional step that allows for services to onboard with a unified schema language alongside services using the existing model.